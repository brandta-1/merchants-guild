import React from 'react';
import ListingForm from '../components/ListingForm';

const Profile = () => {
    return (
        <div>
            <p>Profile</p>
            <ListingForm />
        </div>
    )
};

export default Profile;